# Aster Sports — Session Handoff (2026-06-22)

> Operational handoff for the next session/lane. Reflects `astersports/astersports-web`
> `main` at the time of writing. Governance rules live in `CLAUDE.md` (§1 Flip
> Authority is a HARD RULE); this doc is state + next actions, not a rule source.

## Source of truth
- **`astersports/astersports-web` `main` is canonical** — prod deploys from it via the
  Manus platform. `franksamaritano/astersports-web` is the retired personal build:
  **fully ported, do not merge it wholesale.**
- `main` HEAD at handoff: **`474ea69`**.

## Merged this session (all on `main`)
| PR | What | Commit |
|----|------|--------|
| #68 | Restore dark-posture env guard (`cronSecret.test.ts`) — re: incident #61 | `e330290` |
| #69 | Pagination helper (`shared/paginate.ts`) — port 1/3 | `bfccebc` |
| #72 | Multi-org admin rebuild (replaced auto-closed #71) — port 2/3 | `ac05f3d` |
| #70 | Invite-links atomic-redeem hardening + public join — port 3/3 | `2db174f` |
| #74 | `GET /api/studio/posture` — operator G0 verification endpoint | `e2248ef` |
| #73 | Manus Agent API client (`server/_core/manus/`) + `scripts/manusTask.ts` — **dark/inert** | `474ea69` |

## Personal → org port: COMPLETE (validated)
Verified by file presence + reconciled-content markers on `main`.
**Intentionally excluded** (do NOT "restore" — each is a regression, not a fix):
- recolor / remove ops (org removed them, two-op reduction #54, Architect-signed)
- the polling generation architecture (org uses SSE — `studioStream.ts`)
- `tenants.create` (org removed in M2: open `protectedProcedure` minting credited
  trial tenants with balance↔ledger drift)

Personal PRs #1/#2/#3 were Copilot-review copies of org #16/#17/#18 (already native).
Personal WIP branches (`csrf-samesite`, `infra-hardening`, `live-surface-hardening`,
`governance-claude-md`) were never merged to personal `main` → optional follow-ups only.

## Manus integration — two SEPARATE things
1. **Claude ⇄ Manus via Zapier** (live, account-level). 6 actions enabled:
   `create_task`, `continue_task`, `get_tasks`, `get_task`, `update_task`,
   `delete_task`. Lets Claude dispatch/track Manus tasks from any session that has
   the Zapier connector. `delete_task` is destructive — confirm before use.
2. **App ⇄ Manus client** (`server/_core/manus/`, merged via #73). Server-only,
   **dark**: enable by setting `MANUS_API_KEY` in the app's prod env (Manus platform).
   Not wired into any route or the money path. Operator CLI:
   `MANUS_API_KEY=… pnpm tsx scripts/manusTask.ts "instruction"`.
   The Manus API key grants full account access — treat as a sensitive secret.

## Open carry-forwards (documented inline; not blockers)
1. **G0 — verify prod dark (Frank's hand).** Authoritative read via the #74 endpoint:
   ```bash
   curl -s -H "x-cron-secret: $CRON_SECRET" https://www.astersports.io/api/studio/posture | jq
   ```
   G0 passes when the JSON shows `"dark": true`, all three `flags` false, and
   `effective.scaleLive`/`effective.densityLive` false. Then check the box + log on
   **incident #61**. Context: scale/density were flipped live by an unauthorized
   go-live (`a6b84ad`); the revert was published — this confirms it took.
2. **Create-org re-enablement.** `CreateOrgDialog` ships dark (`VITE_CREATE_ORG_LIVE`
   off; `tenants.create` deliberately not restored). To enable: add a ledger-safe,
   rate-limited `tenants.create` → wire it into the dialog → flip the var (one logged
   flip). Architect-scoped.
3. **Invite-redeem atomicity.** The atomic claim and `grantCredits` are not in one
   shared transaction (`server/routers/inviteLinks.ts`). Architect call.

## Flip Authority (HARD RULE — see CLAUDE.md §1)
No lane sets/changes/deploys any `*_LIVE` flag, `STUDIO_MASK_PROVIDER`, or any prod
secret on the money/mask/sub-processor path. Frank flips by hand on an
Architect-verified SHA after gates clear (G0→G4). Builders PREPARE, never SET.
Re-flip order: recolor (classical) → SAM2 provider → scale → density.

## Housekeeping
- Merged feature branches (`claude/port-*`, `claude/manus-api-client`) can be deleted.
- Incident **#61** stays **open** until G0 is confirmed and logged.
- `MANUS_API_KEY` / `VITE_CREATE_ORG_LIVE` are prepared-but-unset dark switches —
  not flips of a gated money/mask path, but log them if/when set.
